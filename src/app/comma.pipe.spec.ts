/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CommaPipe } from './comma.pipe';

describe('Pipe: Commae', () => {
  it('create an instance', () => {
    let pipe = new CommaPipe();
    expect(pipe).toBeTruthy();
  });
});
